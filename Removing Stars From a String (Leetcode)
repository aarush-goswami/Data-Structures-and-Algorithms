
    char* removeStars(char* s) {
    int len = strlen(s);
    int i,temp=0;
    for(i=0;i<len;i++){
        if(s[i]!='*'){
        s[temp++]= s[i];
        }
        else 
        temp--;
    }
    s[temp]='\0';
    return s;
}
